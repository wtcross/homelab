apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: step-issuer-provisioner-password
  namespace: pki
spec:
  data:
    - remoteRef:
        key: lab-ca-step-issuer-provisioner-password
      secretKey: password
  refreshInterval: 1h0m0s
  secretStoreRef:
    kind: ClusterSecretStore
    name: infra-gcpsm-store
  target:
    name: step-issuer-provisioner-password
    creationPolicy: Owner
