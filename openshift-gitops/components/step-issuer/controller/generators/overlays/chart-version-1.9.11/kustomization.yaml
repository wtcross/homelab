apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  - target:
      kind: HelmChartInflationGenerator
      name: step-issuer
    path: patch-version.yaml
