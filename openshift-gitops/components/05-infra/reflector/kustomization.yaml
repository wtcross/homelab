apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

commonAnnotations:
    argocd.argoproj.io/sync-wave: "5"

helmCharts:
  - name: reflector
    repo: https://emberstack.github.io/helm-charts
    releaseName: reflector
    version: 9.1.45
    valuesFile: helm-values/reflector.yaml
    namespace: reflector
    includeCRDs: true
