apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: keycloak-admin-creds
  namespace: keycloak
spec:
  data:
    - remoteRef:
        key: core-keycloak-admin-user
      secretKey: username
    - remoteRef:
        key: core-keycloak-admin-password
      secretKey: password
    - remoteRef:
        key: core-keycloak-admin-client-id
      secretKey: client-id
    - remoteRef:
        key: core-keycloak-admin-client-secret
      secretKey: client-secret
  refreshInterval: 1h0m0s
  secretStoreRef:
    kind: ClusterSecretStore
    name: infra-gcpsm-store
  target:
    name: keycloak-admin-creds
    creationPolicy: Owner
