---
csiDriver:
  name: "org.democratic-csi.nfs"

storageClasses:
  - name: truenas-nfs-nonroot
    defaultClass: true
    annotations:
      storageclass.kubevirt.io/is-default-virt-class: "true"
      storage.openshift.io/access-mode: "ReadWriteMany"
      storage.openshift.io/access-mode: "ReadWriteOnce"
    labels:
      access-mode: "ReadWriteMany"
      access-mode: "ReadWriteOnce"
    reclaimPolicy: Delete
    volumeBindingMode: Immediate
    allowVolumeExpansion: true
    parameters:
      # for block-based storage can be ext3, ext4, xfs
      # for nfs should be nfs
      fsType: nfs

      # if true, volumes created from other snapshots will be
      # zfs send/received instead of zfs cloned
      # detachedVolumesFromSnapshots: "false"

      # if true, volumes created from other volumes will be
      # zfs send/received instead of zfs cloned
      # detachedVolumesFromVolumes: "false"

    mountOptions:
      - noatime
      - nfsvers=4
    secrets:
      provisioner-secret:
      controller-publish-secret:
      node-stage-secret:
      node-publish-secret:
      controller-expand-secret:

# if your cluster supports snapshots you may enable below
volumeSnapshotClasses:
  - name: truenas-nfs-nonroot
    parameters:
    # if true, snapshots will be created with zfs send/receive
    # detachedSnapshots: "false"
    secrets:
      snapshotter-secret:

node:
  rbac:
    openshift:
      privileged: true
  driver:
    enabled: true
    localtimeHostPath: false
    image: ghcr.io/democratic-csi/democratic-csi:latest
  cleanup:
    image: ghcr.io/containerd/busybox:1.36

controller:
  rbac:
    openshift:
      privileged: true
  driver:
    enabled: true
    image: ghcr.io/democratic-csi/democratic-csi:latest

driver:
  existingConfigSecret: freenas-nfs-driver-config
  config:
    driver: freenas-nfs
    zfs:
      cli:
        sudoEnabled: true
        #  leave paths unset for auto-detection
        paths:
          zfs: /sbin/zfs
          zpool: /sbin/zpool
          sudo: /usr/bin/sudo
          chroot: /usr/sbin/chroot

      # can be used to set arbitrary values on the dataset/zvol
      # can use handlebars templates with the parameters from the storage class/CO
      #datasetProperties:
      #  "org.freenas:description": "{{ parameters.[csi.storage.k8s.io/pvc/namespace] }}/{{ parameters.[csi.storage.k8s.io/pvc/name] }}"
      #  "org.freenas:test": "{{ parameters.foo }}"
      #  "org.freenas:test2": "some value"

      datasetParentName: fast/csi/core.lab.cross.solutions/nfs/vols
      # do NOT make datasetParentName and detachedSnapshotsDatasetParentName overlap
      # they may be siblings, but neither should be nested in the other
      # do NOT comment this option out even if you don't plan to use snapshots, just leave it with dummy value
      detachedSnapshotsDatasetParentName: fast/csi/core.lab.cross.solutions/nfs/snaps
      datasetEnableQuotas: true
      datasetEnableReservation: false
      datasetPermissionsMode: "0777"
      datasetPermissionsUser: 0
      datasetPermissionsGroup: 0
      #datasetPermissionsAcls:
      #- "-m everyone@:full_set:allow"
      #- "-m u:kube:full_set:allow"

    nfs:
      #shareCommentTemplate: "{{ parameters.[csi.storage.k8s.io/pvc/namespace] }}-{{ parameters.[csi.storage.k8s.io/pvc/name] }}"
      shareHost: nas.lab.cross.solutions
      shareAlldirs: false
      shareAllowedHosts: []
      shareAllowedNetworks: []
      shareMaprootUser: root
      shareMaprootGroup: wheel
      shareMapallUser: ""
      shareMapallGroup: ""

csiProxy:
  enabled: true
  image: ghcr.io/democratic-csi/csi-grpc-proxy:latest
