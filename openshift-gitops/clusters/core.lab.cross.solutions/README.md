# TODO

- configure vault as intermediate CA using HSM as root CA
- use 3 master nodes using odroid machines
- use 3 um790 pro's as worker nodes
- add r7615 as worker
- set up SMTP monitoring for UPS(s), NAS, and other relevant systems
  - Evaluate using EDA to respond to power going out

