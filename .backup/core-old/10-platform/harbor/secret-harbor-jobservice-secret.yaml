apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: harbor-jobservice-secret
  namespace: harbor
spec:
  data:
    - remoteRef:
        key: core-harbor-jobservice-secret
      secretKey: secret
  refreshInterval: 1h0m0s
  secretStoreRef:
    kind: ClusterSecretStore
    name: infra-gcpsm-store
  target:
    name: harbor-jobservice-secret
    creationPolicy: Owner
