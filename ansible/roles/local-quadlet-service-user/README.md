# local-quadlet-service-user

Deploy a locked down, user scoped systemd quadlet.

## Role Variables

Check the defaults in [`defaults/main.yml`](defaults/main.yaml).
Check required varaibles in [`tasks/preflight-checks.yml`](tasks/preflight-checks.yaml).
