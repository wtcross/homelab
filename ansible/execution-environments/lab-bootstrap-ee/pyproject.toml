[project]
name = "lab-bootstrap-ee"
version = "0.1.0"
description = "Ansible EE for initial bootstraping of the homelab environment"
readme = "README.md"
requires-python = ">=3.13.8"
dependencies = []

[dependency-groups]
dev = [
    "ansible-builder>=3.1",
]
ee = []
